name: Verify production snapshot

on:
  push:
    branches: [main, sync/production-20260915]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  production-snapshot:
    name: production-snapshot
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm run verify:production
      - run: npm run build
