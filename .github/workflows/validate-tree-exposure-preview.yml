name: Validate TREE exposure preview pipeline

on:
  push:
    branches:
      - feature/leaderboard-exposure-v1
    paths:
      - '.github/workflows/validate-tree-exposure-preview.yml'

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/leaderboard-exposure-v1
          fetch-depth: 0

      - name: Add deterministic cache-order validation and corrected endpoint fixture
        shell: bash
        run: |
          set -euo pipefail
          python scripts/patch-tree-exposure-cache-order.py
          rm -f scripts/patch-tree-exposure-cache-order.py .github/workflows/validate-tree-exposure-preview.yml
          git diff --check

      - name: Build and run complete test suite
        shell: bash
        run: |
          set -euo pipefail
          npm ci
          npm run build
          node --check dapp/app.js
          node tests/dashboard-cache.test.mjs
          node tests/leaderboard-ui-state.test.mjs
          for test in tests/*.ts; do
            echo "Running $test"
            npx --yes tsx "$test"
          done
          git diff --check

      - name: Commit verified preview pipeline
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "TheCryptoArborist"
          git config user.email "190985450+TheCryptoArborist@users.noreply.github.com"
          git add -A
          git commit -m "Add complete-only TREE exposure preview pipeline"
          git push origin HEAD:feature/leaderboard-exposure-v1
